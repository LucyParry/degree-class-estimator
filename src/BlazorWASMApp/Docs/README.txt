RUNNING LOCALLY
---------------------------------
Default is to the root directory but since we're using GitHub pages, run from cmd with name of repo, e.g:

dotnet run --pathbase=/degree-class-estimator